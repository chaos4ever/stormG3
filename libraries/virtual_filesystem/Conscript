# $chaos: Conscript,v 1.1 2002/07/21 12:38:40 per Exp $ -*- Perl -*-

# Abstract: CONS script for the virtual file system library.
# Author: Per Lundberg <per@chaosdev.org>

# Copyright 2002 chaos development.
# Use freely under the terms listed in the file COPYING.

$library_binary = 'libvirtual_filesystem.a';

Import qw(cons library_static_directory);
Library $cons $library_binary, 'virtual_filesystem.c';
Command $cons 'install', $library_binary,
  qq(mkdir -p $library_static_directory
     install virtual_filesystem/$library_binary $library_static_directory);

# FIXME: Install headers also. We don't need it yet though.
